% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotStats.R
\name{plotStatsBar}
\alias{plotStatsBar}
\title{Bar plot of cluster statistics for cells inside boundaries or ring regions}
\usage{
plotStatsBar(
  cell_stats = NULL,
  stat_column = c("proportion", "count"),
  colors = colors15_cheng,
  angle_x_label = 0,
  theme_ggplot = theme_spneigh()
)
}
\arguments{
\item{cell_stats}{A data frame containing summarized cell statistics,
typically the output from \code{statsCellsInside()}.
Must include columns \code{region_id}, \code{cluster},
and the specified \code{stat_column}.}

\item{stat_column}{Character. Column name in \code{cell_stats} to
use for the y-axis.
Options are \code{"count"} (number of cells) or \code{"proportion"}
(relative fraction per region).}

\item{colors}{A vector of cluster colors. Default uses \code{colors15_cheng}.}

\item{angle_x_label}{Numeric angle (in degrees) to rotate the x-axis labels.
Useful for improving label readability in faceted
or dense plots. Default is 0 (no rotation).}

\item{theme_ggplot}{A ggplot2 theme object. Default is \code{theme_spneigh()}.}
}
\value{
A \code{ggplot2} object showing a faceted bar plot of cell statistics
per region.
}
\description{
Creates a bar plot to visualize the distribution of cells inside
spatial regions (e.g., boundaries or rings),
either as raw counts or proportions per cluster.
The plot is faceted by \code{region_id} to show statistics across
multiple spatial subregions.
}
\examples{
coords <- readRDS(system.file("extdata", "MouseBrainCoords.rds",
    package = "SpNeigh"
))

boundary_points <- getBoundary(
    data = coords, one_cluster = 2,
    eps = 120, minPts = 10
)
cells_inside <- getCellsInside(data = coords, boundary = boundary_points)
stats_cells <- statsCellsInside(cells_inside)

plotStatsBar(stats_cells, stat_column = "proportion")
plotStatsBar(stats_cells, stat_column = "count")

}
