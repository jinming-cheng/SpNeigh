% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotInteractionMatrix.R
\name{plotInteractionMatrix}
\alias{plotInteractionMatrix}
\title{Plot a heatmap of a row-scaled spatial interaction matrix}
\usage{
plotInteractionMatrix(
  interaction_matrix = NULL,
  low_color = "blue",
  mid_color = "white",
  high_color = "red",
  angle_x_label = 45,
  title = "Row-Scaled Interaction Matrix (Z-scores)"
)
}
\arguments{
\item{interaction_matrix}{A numeric matrix with focal clusters as rows
and neighbor clusters as columns.
Typically the output from
\code{computeSpatialInteractionMatrix()}.}

\item{low_color}{Color representing low z-score values. Default is \code{"blue"}.}

\item{mid_color}{Color representing the midpoint (z-score = 0).
Default is \code{"white"}.}

\item{high_color}{Color representing high z-score values. Default is \code{"red"}.}

\item{angle_x_label}{Angle (in degrees) to rotate x-axis labels.
Default is \code{45}.}

\item{title}{Title for the heatmap.}
}
\value{
A \code{ggplot} object representing the row-scaled heatmap
of the interaction matrix.
}
\description{
Visualizes a spatial interaction matrix using a heatmap,
where rows represent focal clusters and columns represent neighbor clusters.
Each row is scaled using z-scores to highlight
relative enrichment patterns across neighbor types.
This is useful for detecting spatial proximity patterns
between cell populations.
}
\examples{
coords <- readRDS(system.file("extdata", "MouseBrainCoords.rds",
    package = "SpNeigh"
))

boundary_points <- getBoundary(
    data = coords, one_cluster = 2,
    eps = 120, minPts = 10
)
ring_regions <- getRingRegion(boundary = boundary_points, dist = 100)
cells_ring <- getCellsInside(data = coords, boundary = ring_regions)
coords_sub <- subset(coords, cell \%in\% cells_ring$cell)
interaction_matrix <- computeSpatialInteractionMatrix(coords_sub)

plotInteractionMatrix(interaction_matrix)

}
